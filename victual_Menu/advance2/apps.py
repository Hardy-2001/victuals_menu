from django.apps import AppConfig


class Advance2Config(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'advance2'

    # 🎯 THE INSTANT RE-LABEL ENGINE:
    # This overrides the sidebar menu title box layout completely on your dashboard screens!
    verbose_name = "MODERN2"
